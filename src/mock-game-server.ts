
const mockGameServer = (url: string) => {
    // We're just going to simulate a game server

    setTimeout(() => {
        // Randomly change player counts every 3 seconds
    }, 3000);
};

export { mockGameServer };
